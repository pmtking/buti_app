"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EXTRA_UTILITIES_CSS = void 0;
const toKebabCase = str => str.replace(/[A-Z]/g, letter => `-${letter.toLowerCase()}`);
const generateExtraUtilities = map => {
  return Object.entries(map).map(([name, style]) => [`@utility ${name} {`, ...Object.entries(style).map(([key, value]) => `  ${toKebabCase(key)}: ${value};`), `}`, ""].join("\n")).join("\n");
};
const EXTRA_UTILITIES_MAP = {
  "border-continuous": {
    borderCurve: "continuous"
  },
  "border-circular": {
    borderCurve: "circular"
  }
};
const EXTRA_UTILITIES_CSS = exports.EXTRA_UTILITIES_CSS = generateExtraUtilities(EXTRA_UTILITIES_MAP);