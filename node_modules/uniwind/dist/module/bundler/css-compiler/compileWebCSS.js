import { transform } from "lightningcss";
export const compileWebCSS = (bundlerConfig, tailwindCSS) => {
  return transform({
    code: Buffer.from(tailwindCSS),
    filename: "uniwind.css",
    visitor: bundlerConfig.cssVisitor
  }).code.toString();
};
