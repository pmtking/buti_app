import type { SharedValue } from "react-native-reanimated";
type Range = [number, number];
export interface VisibleRanges {
    negativeRange: Range;
    positiveRange: Range;
}
export type VisibleRangesValue = SharedValue<VisibleRanges>;
export declare function computeVisibleRanges(params: {
    total: number;
    windowSize?: number;
    currentIndex: number;
    loop?: boolean;
}): VisibleRanges;
export declare function useVisibleRanges(options: {
    total: number;
    viewSize: number;
    windowSize?: number;
    translation: SharedValue<number>;
    loop?: boolean;
}): VisibleRangesValue;
export {};
//# sourceMappingURL=useVisibleRanges.d.ts.map