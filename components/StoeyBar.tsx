import { View } from "react-native"



export const StoeyBar: React.FC<Props> = ({ isOpen, onClose, onNavigate }) => {
    return (
        <>
         <View style={styles.container}>
            
         </View>
        </>
    )
}

const styles = StyleSheet.create({
    container:{
        flexDrirection: 'row',
        margin:'auto',
    }
})